# AMOV P450 Jetson Xavier NX 週進度報告

日期：2026-07-20（Asia/Taipei）  
狀態：Jetson 已由 eMMC 成功進入 Ubuntu 圖形介面。

## 本週目標

修復 Jetson Xavier NX 的 JetPack 5.1.4 開機問題，完成 BSP rootfs、QSPI bootloader 與系統儲存媒體刷寫，讓 P450 後續可以進行 Pixhawk、ROS 2 與 PX4 整合。

## 問題起點

先前將 JP5.1.4 SD 映像寫入兩張 SD 卡後，Jetson 進入 UEFI Shell。Boot Manager 中的本機 boot 項目無法啟動，最後進入 PXE/HTTP fallback。

初期根據舊系統資訊把硬體判定為 P3668-0000 microSD 版本，因此先執行 QSPI-only 刷寫；後續證據證明此判定過時。

## 診斷結果

Recovery EEPROM 與刷寫日志反覆顯示：

```text
Board ID(3668) version(301) sku(0001) revision(G.0)
```

UEFI `map -r` 顯示：

```text
eMMC(0x0)
FS2:
BLK0 ～ BLK13
```

進入 `FS2:` 後可看到完整舊 Linux rootfs。結論是實際模組為 P3668-0001 eMMC 版本，並非原先判定的 P3668-0000 microSD 版本。

NVIDIA 對應設定為：

```text
jetson-xavier-nx-devkit-emmc mmcblk0p1
```

## 修復內容

### 1. 修復 Python 相容命令

原先 `flash.sh` 找不到 `python`，造成 QSPI boot blob 的 CRC32 欄位空白。已安裝：

```bash
sudo apt-get install -y python-is-python3
```

修復後產生：

```text
BYTES:85 CRC32:9DE52483
```

### 2. 重新建立完整 rootfs

原本 rootfs 不完整，`etc` 不是目錄，且缺少 `bin`、`lib` 等內容。重新解壓官方 R35.6.0 sample rootfs 後執行：

```bash
sudo ./apply_binaries.sh
```

結果：

```text
L4T BSP package installation completed!
Success!
```

### 3. QSPI-only 刷寫

QSPI-only 刷寫完成，確認 R35.6.0 UEFI、BCT 與 bootloader 可正常寫入。

### 4. eMMC 完整刷寫

執行：

```bash
sudo ./flash.sh jetson-xavier-nx-devkit-emmc mmcblk0p1 \
  2>&1 | tee ~/xavier_nx_emmc_clean.log
```

日志確認：

```text
Name: jetson-xavier-nx-devkit-emmc
flash_l4t_t194_spi_emmc_p3668.xml
system.img built successfully.
Writing partition APP with system.img
Writing partition esp with esp.img
Flashing completed
*** The target t186ref has been flashed successfully. ***
Coldbooting the device
```

## 最終成果

- BSP rootfs 已修復。
- Python/CRC32 問題已排除。
- QSPI 已完成 R35.6.0 bootloader 刷寫。
- eMMC APP、kernel、recovery、ESP、BCT 與韌體分割區已完成刷寫。
- Jetson 已成功由 eMMC 進入 Ubuntu 圖形介面。
- 原本 128 GB 舊 microSD 未被格式化。

## 下週後續作業

1. 驗證 Ubuntu、L4T、Kernel 與 eMMC root device。
2. 驗證 Ethernet、Wi-Fi 與 SSH。
3. 連接 Pixhawk 6C，確認 USB/UART。
4. 建立 Ubuntu 22.04／ROS 2 Humble ARM64 容器。
5. 安裝 Micro XRCE-DDS Agent。
6. 建置 `px4_msgs`、`px4_ros_com`。
7. 驗證 `/fmu/out/*` topic 與 PX4 ULog。
8. 無槳測試 Kill Switch、定高、定點與失聯處置。
9. 最後才進行起飛、短暫停留與降落測試。

## 主機日志位置

```text
/home/wilson/xavier_nx_qspi_rerun.log
/home/wilson/xavier_nx_qspi_clean.log
/home/wilson/xavier_nx_emmc_clean.log
```

上述日志未提交 Git，因 `.gitignore` 排除 `*.log`；若需要跨電腦保存，應另外使用受控的 artifact 儲存方式，不要直接把完整刷寫日志提交到公開 repository。
