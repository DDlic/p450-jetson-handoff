# P450 PX4 v1.15.4 XRCE 實機測試（2026-08-04）

## 測試基線

- 飛控：Pixhawk 6C／PX4FMUv6C
- 韌體：官方 PX4 v1.15.4 source build
- source commit：`99c40407ffd7ac184e2d7b4b293f36f10fe561ef`
- 韌體 SHA-256：`21af0b94edd5de84dde5360874d8e1f66a52e3be07dfbafaaffc03baa580c29a`
- NX：Jetson Xavier NX／Ubuntu 20.04／ROS 2 Foxy
- Agent：Micro XRCE-DDS Agent v2.4.2
- transport：`/dev/ttyTHS1`／460800 baud／TELEM2
- `px4_msgs`：官方 `release/1.15`，commit
  `a1045ec4feb6d709bdecaf3895f1d5b43a5dabb8`
- ROS workspace：`/media/p450/P450_DATA/builds/p450_ros2_ws_v115`

v1.15.4 `dds_topics.yaml` 使用的 43 個 message types 已逐一與上述 `px4_msgs`
比對，結果為 `MATCHED=43`、`MISSING=0`、`DIFFERENT=0`。刷入後可建立 43 個
`/fmu/*` topics；這不是 ROS message version mismatch。

所有測試均保持未解鎖，取樣時 `VehicleStatus.arming_state=1`、`failsafe=false`。
沒有發布模式切換、解鎖、姿態、速率、位置、馬達或推力命令。

## stock v1.15.4 純接收結果

60 秒 `SensorCombined` 唯讀監測：

```text
messages=2737
average_hz=45.439
median_gap_ms=1.444
max_gap_ms=1014.823
gaps_over_100ms=15
gaps_over_500ms=15
gaps_over_1s=5
result=FAIL
```

45 秒多 topic 關聯測試顯示 `SensorCombined`、`VehicleAttitude` 與
`VehicleOdometry` 在相同時刻一起停約 1 秒；三者的 PX4 source timestamp 也直接
跳過約 1 秒，不是 ROS subscriber 單獨漏收或 NX callback 排程延遲。

```text
sensor:   max_arrival_ms=1003.033 max_source_ms=1013.504
attitude: max_arrival_ms=1002.717 max_source_ms=1023.392
odometry: max_arrival_ms=1004.167 max_source_ms=1028.363
```

## Agent lifecycle

暫停 systemd Agent，以相同裝置與 baud 啟動 `-v 6` Agent，再做 45 秒唯讀測試：

```text
messages=1990
average_hz=44.152
max_gap_ms=1002.695
gaps_over_500ms=11
create_client=1
session established=1
delete_client=0
session closed=0
```

因此這次不是整個 session close/recreate；約 1 秒停頓發生在仍存在的 session 內。
Agent 服務本身全程沒有 crash 或自動 restart。

## 排除時間同步

由 QGC 將 `UXRCE_DDS_SYNCT` 從預設值 `1` 暫設為 `0` 並重啟。重啟後
`TimesyncStatus` 不再發布，證明設定生效；42 個其餘 `/fmu/*` topics 正常建立。

60 秒純接收結果：

```text
messages=2488
average_hz=41.405
median_gap_ms=1.071
max_gap_ms=1003.738
gaps_over_500ms=20
gaps_over_1s=3
result=FAIL
```

停用 timestamp synchronization 沒有改善，因此 `UXRCE_DDS_SYNCT` 不是約 1 秒
空窗的原因。此參數目前仍為診斷值 `0`；完成候選韌體 A/B 後必須恢復為 `1` 並重測，
不可把停用時間同步當成最終飛行設定。

## NX→PX4 非控制 payload 測試

使用 `/fmu/in/onboard_computer_status` 發布非控制性的 companion status；QoS 與
PX4 reader 均為 best-effort，Agent 詳細日誌的 `DataReader::read_fn` 證明 DDS payload
確實到達 Agent 並送往 serial。

2 Hz 測試期間 Agent 收到 84 筆 status，但 35 秒輸出仍為：

```text
messages=1702
average_hz=48.606
max_gap_ms=1052.668
gaps_over_500ms=6
gaps_over_1s=6
result=FAIL
```

提高到 20 Hz 後，35 秒內 `SensorCombined` 為 0 筆。heartbeat 停止後仍未自行恢復；
Agent active、UART holder 正常，但 12 秒仍為 0 筆。重啟 Agent 後 PX4 client 才重新
連線並恢復資料，恢復後仍有約 1 秒空窗。

## 原始碼對照與候選修補

stock v1.15.4 的主迴圈每輪只執行一次：

```cpp
uxr_run_session_timeout(&session, 0);
```

PX4 官方提交
[`d12a7dd11da5`](https://github.com/PX4/PX4-Autopilot/commit/d12a7dd11da521ebbdd6ba07be1987b459d39ace)
明確指出，每輪只處理一次 session 會造成接收資料顯著延遲，甚至讓已註冊飛行模式
timeout；官方修改為每輪最多處理 10 次，直到沒有新 payload。該提交雖位於
v1.15.4 ancestry 中，但後續重構後 v1.15.4 實際 source 又只剩一次處理。

已從 stock v1.15.4 建立最小回補候選版：

```text
firmware/p450-pixhawk6c-v1.15.4-xrce-rx-drain-996b1df7a1.px4
SHA-256: dbfd43085bbb4fe59744ad244a973b1243fb55d34ed36df52c9a0855be464949
source: 996b1df7a10a35b3e3534df9c5629f3675c7cab0
```

此版只有 11 行新增、2 行替換，只改 XRCE receive draining；完整編譯成功，board ID
56、image size 1,961,732 bytes、FLASH 99.78%。

## 2026-08-05 候選版刷入後結果

機主已刷入 `996b1df7a1`，第一次 A/B 保留 `UXRCE_DDS_SYNCT=0`。測試前清潔重啟
Agent；只有 Agent 占用 `/dev/ttyTHS1`，42 個 `/fmu/*` topics 正常，沒有新增
UART／DMA kernel error，飛控維持 `arming_state=1`、`failsafe=false`。

60 秒純接收第一關：

```text
messages=2411
average_hz=40.138
first_sample_ms=26.088
trailing_gap_ms=0.143
median_gap_ms=1.002
max_gap_ms=1005.408
max_source_gap_ms=1021.902
gaps_over_100ms=22
gaps_over_500ms=22
gaps_over_1s=7
result=FAIL
```

Agent 測試前後均為 PID 5418、`NRestarts=0`、`ExecMainStatus=0`；42 個 topics 沒有
消失。這表示最小 `d12a7dd` receive-drain 回補沒有改善零 NX→PX4 payload 時的約
1 秒停頓，且未通過 Phase 3 第一個 continuity gate。依停止條件沒有發布 2 Hz／
20 Hz `OnboardComputerStatus`。候選版不得標記為可飛或 XRCE 已修復。

## 候選版刷入後順序

1. 保持拆槳、未解鎖、穩定供電。
2. 第一次 A/B 先保留 `UXRCE_DDS_SYNCT=0`，避免一次改兩個變因。
3. 重啟 Agent，做 60 秒純接收測試。
4. 發布 2 Hz `OnboardComputerStatus` 並做 60 秒輸出 gap 測試。
5. 只有前一步通過，才做 20 Hz 非控制 status 壓力測試。
6. 通過後將 `UXRCE_DDS_SYNCT` 恢復為 `1`、重啟飛控並重做以上測試。
7. 未通過 10 分鐘 continuity 與 Offboard heartbeat 前，不解鎖、不裝槳、不起飛。
